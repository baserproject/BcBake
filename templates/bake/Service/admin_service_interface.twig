{#
/**
 * baserCMS :  Based Website Development Project <https://basercms.net>
 * Copyright (c) NPO baser foundation <https://baserfoundation.org/>
 *
 * @copyright     Copyright (c) NPO baser foundation
 * @link          https://basercms.net baserCMS Project
 * @since         5.0.7
 * @license       https://basercms.net/license/index.html MIT License
 */
#}
{{ element('Bake.file_header', {
    namespace: "#{namespace}\\Service\\Admin",
    classImports: (plugin or prefix) ? [
    ] : [],
}) }}

/**
 * {{ name }} Admin Service Interface
 */
interface {{ name }}AdminServiceInterface extends \{{ namespace }}\Service\{{ name }}ServiceInterface
{

    /**
     * get view vars for index
     * @param \Cake\Datasource\ResultSetInterface ${{ pluralName }}
     * @return array
     */
    public function getViewVarsForIndex(\Cake\Datasource\ResultSetInterface ${{ pluralName }}): array;

    /**
     * get view vars for add
     * @param \Cake\Datasource\EntityInterface ${{ singularName }}
     * @return array
     */
    public function getViewVarsForAdd(\Cake\Datasource\EntityInterface ${{ singularName }}): array;

    /**
     * get view vars for edit
     * @param \Cake\Datasource\EntityInterface ${{ singularName }}
     * @return array
     */
    public function getViewVarsForEdit(\Cake\Datasource\EntityInterface ${{ singularName }}): array;

    /**
     * get view vars for view
     * @return array
     */
    public function getViewVarsForView(\Cake\Datasource\EntityInterface ${{ singularName }}): array;

}